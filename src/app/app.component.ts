import { Component } from '@angular/core';

@Component({
  selector: 'app-root', // Quando vc define um seletor para determinado componente vc passa a referenciar dentro do HTML a partir desse seletor.
  templateUrl: 'app.component.html'
})
export class AppComponent {
  
}
